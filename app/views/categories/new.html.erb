<h1>カテゴリ登録</h1>

<%= form_with model: @category, local: true do |f| %>
  <div>
    <%= f.label :category, 'カテゴリ名' %>
    <%= f.text_field :category %>
  </div>
  <div>
    <%= f.submit %>
  </div>
<% end %>